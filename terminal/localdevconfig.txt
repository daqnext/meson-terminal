#token register and login in https://meson.network
token = XhTu7Cw/wfUzUL4shA1BBQ==

# server port
port = 19091

# space limit (Maximum allowable space for Terminal in GB. Less than the total disk. At least 10GB)
spacelimit = 2

#server address e.x. http://127.0.0.1:9090 #include port
server = http://coldcdn.com

#apiProto http/https
#apiProto = http

#loglevel: PanicLevel=0,FatalLevel=1,ErrorLevel=2,WarnLevel=3,InfoLevel=4,DebugLevel=5,TraceLevel=6
loglevel= 6

#ginMode debug/release
ginMode = debug

#runMode local/server
runMode = local
